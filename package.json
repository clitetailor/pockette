{
  "name": "pockette",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm build:dev && serve dist",
    "build": "pnpm --filter @pockette/* build",
    "build:dev": "pnpm --filter @pockette/* build:dev",
    "format": "pnpm --filter @pockette/* format",
    "test": "pnpm --filter @pockette/* test",
    "e2e": "pnpm --filter @pockette/* e2e"
  },
  "author": "clitetailor",
  "license": "MIT",
  "dependencies": {
    "serve": "^11.3.2"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "prettier-plugin-svelte": "^1.1.0"
  }
}
